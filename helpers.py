import datetime


def add_credit(source, amount, bucket):
    print("adding credit")
    time = getCurrentTimeStamp()

def add_debit(source, amount, bucket):
    print("adding debit")
    time = getCurrentTimeStamp()

def getCurrentTimeStamp():
    time = datetime.time
    print("The time now is:" + time)
    return time
